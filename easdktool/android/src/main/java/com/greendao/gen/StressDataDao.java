package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.easdktool.db.StressData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "STRESS_DATA".
*/
public class StressDataDao extends AbstractDao<StressData, Long> {

    public static final String TABLENAME = "STRESS_DATA";

    /**
     * Properties of entity StressData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Time_stamp = new Property(0, long.class, "time_stamp", true, "_id");
        public final static Property Stess_value = new Property(1, int.class, "stess_value", false, "STESS_VALUE");
        public final static Property PressureLevel = new Property(2, int.class, "pressureLevel", false, "PRESSURE_LEVEL");
    }


    public StressDataDao(DaoConfig config) {
        super(config);
    }
    
    public StressDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"STRESS_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: time_stamp
                "\"STESS_VALUE\" INTEGER NOT NULL ," + // 1: stess_value
                "\"PRESSURE_LEVEL\" INTEGER NOT NULL );"); // 2: pressureLevel
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"STRESS_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, StressData entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getTime_stamp());
        stmt.bindLong(2, entity.getStess_value());
        stmt.bindLong(3, entity.getPressureLevel());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, StressData entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getTime_stamp());
        stmt.bindLong(2, entity.getStess_value());
        stmt.bindLong(3, entity.getPressureLevel());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public StressData readEntity(Cursor cursor, int offset) {
        StressData entity = new StressData( //
            cursor.getLong(offset + 0), // time_stamp
            cursor.getInt(offset + 1), // stess_value
            cursor.getInt(offset + 2) // pressureLevel
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, StressData entity, int offset) {
        entity.setTime_stamp(cursor.getLong(offset + 0));
        entity.setStess_value(cursor.getInt(offset + 1));
        entity.setPressureLevel(cursor.getInt(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(StressData entity, long rowId) {
        entity.setTime_stamp(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(StressData entity) {
        if(entity != null) {
            return entity.getTime_stamp();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(StressData entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
