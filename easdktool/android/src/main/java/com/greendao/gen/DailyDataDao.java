package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.easdktool.db.DailyData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DAILY_DATA".
*/
public class DailyDataDao extends AbstractDao<DailyData, Long> {

    public static final String TABLENAME = "DAILY_DATA";

    /**
     * Properties of entity DailyData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Time_stamp = new Property(0, long.class, "time_stamp", true, "_id");
        public final static Property Steps = new Property(1, int.class, "steps", false, "STEPS");
        public final static Property Calorie = new Property(2, int.class, "calorie", false, "CALORIE");
        public final static Property Distance = new Property(3, int.class, "distance", false, "DISTANCE");
        public final static Property Duration = new Property(4, int.class, "duration", false, "DURATION");
        public final static Property Average_heart_rate = new Property(5, int.class, "average_heart_rate", false, "AVERAGE_HEART_RATE");
    }


    public DailyDataDao(DaoConfig config) {
        super(config);
    }
    
    public DailyDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DAILY_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: time_stamp
                "\"STEPS\" INTEGER NOT NULL ," + // 1: steps
                "\"CALORIE\" INTEGER NOT NULL ," + // 2: calorie
                "\"DISTANCE\" INTEGER NOT NULL ," + // 3: distance
                "\"DURATION\" INTEGER NOT NULL ," + // 4: duration
                "\"AVERAGE_HEART_RATE\" INTEGER NOT NULL );"); // 5: average_heart_rate
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DAILY_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, DailyData entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getTime_stamp());
        stmt.bindLong(2, entity.getSteps());
        stmt.bindLong(3, entity.getCalorie());
        stmt.bindLong(4, entity.getDistance());
        stmt.bindLong(5, entity.getDuration());
        stmt.bindLong(6, entity.getAverage_heart_rate());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, DailyData entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getTime_stamp());
        stmt.bindLong(2, entity.getSteps());
        stmt.bindLong(3, entity.getCalorie());
        stmt.bindLong(4, entity.getDistance());
        stmt.bindLong(5, entity.getDuration());
        stmt.bindLong(6, entity.getAverage_heart_rate());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public DailyData readEntity(Cursor cursor, int offset) {
        DailyData entity = new DailyData( //
            cursor.getLong(offset + 0), // time_stamp
            cursor.getInt(offset + 1), // steps
            cursor.getInt(offset + 2), // calorie
            cursor.getInt(offset + 3), // distance
            cursor.getInt(offset + 4), // duration
            cursor.getInt(offset + 5) // average_heart_rate
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, DailyData entity, int offset) {
        entity.setTime_stamp(cursor.getLong(offset + 0));
        entity.setSteps(cursor.getInt(offset + 1));
        entity.setCalorie(cursor.getInt(offset + 2));
        entity.setDistance(cursor.getInt(offset + 3));
        entity.setDuration(cursor.getInt(offset + 4));
        entity.setAverage_heart_rate(cursor.getInt(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(DailyData entity, long rowId) {
        entity.setTime_stamp(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(DailyData entity) {
        if(entity != null) {
            return entity.getTime_stamp();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(DailyData entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
