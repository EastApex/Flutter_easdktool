package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.easdktool.db.SleepScore;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SLEEP_SCORE".
*/
public class SleepScoreDao extends AbstractDao<SleepScore, Long> {

    public static final String TABLENAME = "SLEEP_SCORE";

    /**
     * Properties of entity SleepScore.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Sleep_score = new Property(0, int.class, "sleep_score", false, "SLEEP_SCORE");
        public final static Property StartTime = new Property(1, long.class, "startTime", true, "_id");
        public final static Property EndTime = new Property(2, long.class, "endTime", false, "END_TIME");
    }


    public SleepScoreDao(DaoConfig config) {
        super(config);
    }
    
    public SleepScoreDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SLEEP_SCORE\" (" + //
                "\"SLEEP_SCORE\" INTEGER NOT NULL ," + // 0: sleep_score
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 1: startTime
                "\"END_TIME\" INTEGER NOT NULL );"); // 2: endTime
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SLEEP_SCORE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SleepScore entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getSleep_score());
        stmt.bindLong(2, entity.getStartTime());
        stmt.bindLong(3, entity.getEndTime());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SleepScore entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getSleep_score());
        stmt.bindLong(2, entity.getStartTime());
        stmt.bindLong(3, entity.getEndTime());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 1);
    }    

    @Override
    public SleepScore readEntity(Cursor cursor, int offset) {
        SleepScore entity = new SleepScore( //
            cursor.getInt(offset + 0), // sleep_score
            cursor.getLong(offset + 1), // startTime
            cursor.getLong(offset + 2) // endTime
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SleepScore entity, int offset) {
        entity.setSleep_score(cursor.getInt(offset + 0));
        entity.setStartTime(cursor.getLong(offset + 1));
        entity.setEndTime(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(SleepScore entity, long rowId) {
        entity.setStartTime(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(SleepScore entity) {
        if(entity != null) {
            return entity.getStartTime();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(SleepScore entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
