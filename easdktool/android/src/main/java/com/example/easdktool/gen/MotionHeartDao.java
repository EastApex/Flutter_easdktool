package com.example.easdktool.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.easdktool.db.MotionHeart;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "MOTION_HEART".
*/
public class MotionHeartDao extends AbstractDao<MotionHeart, Long> {

    public static final String TABLENAME = "MOTION_HEART";

    /**
     * Properties of entity MotionHeart.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property StampTime = new Property(0, long.class, "stampTime", true, "_id");
        public final static Property MotionHr = new Property(1, int.class, "motionHr", false, "MOTION_HR");
    }


    public MotionHeartDao(DaoConfig config) {
        super(config);
    }
    
    public MotionHeartDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"MOTION_HEART\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: stampTime
                "\"MOTION_HR\" INTEGER NOT NULL );"); // 1: motionHr
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MOTION_HEART\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, MotionHeart entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getStampTime());
        stmt.bindLong(2, entity.getMotionHr());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, MotionHeart entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getStampTime());
        stmt.bindLong(2, entity.getMotionHr());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public MotionHeart readEntity(Cursor cursor, int offset) {
        MotionHeart entity = new MotionHeart( //
            cursor.getLong(offset + 0), // stampTime
            cursor.getInt(offset + 1) // motionHr
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, MotionHeart entity, int offset) {
        entity.setStampTime(cursor.getLong(offset + 0));
        entity.setMotionHr(cursor.getInt(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(MotionHeart entity, long rowId) {
        entity.setStampTime(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(MotionHeart entity) {
        if(entity != null) {
            return entity.getStampTime();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(MotionHeart entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
